dataset:
  zju_mocap_path: ./
  subject: '393'
  sex: 'neutral'

training_view: 0
max_frames: -1
eval_view: 1-22  # camera index, from Camera_B2 to Camera_B23 ########
eval_skip: 30  # choose image per 30 frame while eval #################

output:
  dir: ../datasets/zju_mocap
  name: '393'

eval_output:
  dir: ../datasets/zju_mocap
  name: '393_eval'  